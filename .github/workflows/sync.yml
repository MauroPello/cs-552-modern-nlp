name: Sync Fork

on:
  schedule:
    - cron: '0 12 * * *'   # every day at 11 AM UTC
  workflow_dispatch:        # lets you trigger manually from Actions tab

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: tgymnich/fork-sync@v2.0
        with:
          owner: epfl-nlp
          repo: cs-552-modern-nlp
          base: main
          head: main